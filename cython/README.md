# This directory contains Cython files for virt-manager-win.
the <code>raw</code> directory contains raw source files, you will need to compile them into Cython modules before using them. You can do this by running the build script in the <code>utils</code> directory in the projecr root:

```
Build tool WIP
```
